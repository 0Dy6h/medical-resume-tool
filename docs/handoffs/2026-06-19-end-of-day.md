# End-of-Day Handoff — 2026-06-19

## Goal

Three-phase session as lead developer:
1. Product review (what to build, P0/P1/P2).
2. Execution-readiness research (how, with what — deps, prior art, skills, preconditions).
3. Implement the highest-priority remediations that can be **fully verified by real runs** this session, defer the rest with reasons.

Closing after shipping the verifiable subset (submittable export + product-honesty fixes) and documenting the blocked #1 priority.

## Current state

- Branch `main`. Two new commits this session (one `feat`, one `docs`). **Not pushed.**
- Backend: `uv run --project backend pytest -q` → **99 passed**.
- Frontend: `pnpm test` **13 passed**, `pnpm typecheck`, `pnpm build` all green.
- Live site **unchanged** (no deploy this session): `http://110.42.136.106/`, still HTTP-only.

## Completed (each change traceable to a product recommendation)

- **G1 / P0-2 — Submittable export.** Resume output was a header-less bullet list and the DOCX was hand-rolled XML.
  - Profile gained a `basics` block (姓名 / 电话 / 邮箱 / 求职意向 / 所在地 / 个人简介) — schema, storage default, frontend "个人信息" form, types, demo profile.
  - Resume generation prepends an `identity` section (name + contact) when basics are filled; falls back to old layout otherwise.
  - `exporter.py` rewritten with **python-docx** (the already-installed dep) — name as a centered header, contact line, sections as headings + bullets. PDF (`fpdf2`) renders the same header.
- **G2 / P1-4 — Demo-data honesty.** `isFixture` sources now show a "演示数据" badge in the crawl table + an honest subtitle, so users can't mistake fixtures for real crawls. New `isDemoStrategy` helper + unit test.
- **G3 / P1-12 — Auth-secret warning.** `auth.py` logs a startup warning when `AUTH_SECRET` is unset (tokens won't survive a restart).
- **G4 / P1-5 — Matcher synonyms.** Added an **additive** medical synonym map to the requirement↔fact token matcher (e.g. a "统计学/数据处理" requirement now matches an "SPSS/数据分析" fact). Strictly additive → no regression to existing evidence behavior.

## Verification

- Backend `pytest`: **99 passed** (93 baseline + 6 new in `test_resume.py` and `test_core_flow.py`: identity export, basics persistence, identity-section presence/absence, synonym match).
- Frontend: vitest **13 passed**, `tsc -b` clean, `vite build` OK.
- Real run (beyond the harness): generated a DOCX and **re-opened it with python-docx** — valid Word document, name present in the header; PDF is a valid `%PDF` (~48 KB, embedded CJK font).

## Still open / deferred (with reasons — not skipped silently)

- **P0-1 — LLM grounded rewriting (the #1 product priority): BLOCKED on user decisions.** Needs an Anthropic API key, a token budget, and a reachability plan (api.anthropic.com is not directly reachable from the China CVM). Implementing it now would be an unauthorized outward/cost integration that can't be real-run-verified, so it was deferred.
  - **Seam is ready:** the `basics`→`identity` data chain plus the existing per-evidence `profile_field_id` anchoring are in place. Drop-in point = the item-text construction in `build_resume_sections` (`backend/app/services/resume.py`): rewrite each bullet from the user's real fact, aligned to the JD, keeping the truth constraint and the `profile_field_id` link. Use the `claude-api` skill (anthropic SDK, `messages.parse` + Pydantic, prompt caching, model routing Opus 4.8 / Sonnet 4.6 / Haiku 4.5).
- **P0-3 — HTTPS/TLS + fixed `AUTH_SECRET` on the server: server ops, not repo code.** Site is still HTTP — **do not enter real PII on the public demo.** Needs a domain + nginx/caddy on the CVM.
- Broader slices from the prior handoff remain deferred: parser/data-quality review queue, richer export templates, import→basics population, more official-site adapters.

## Recommended next step

Decide the LLM provider/reachability + budget for P0-1, then implement grounded rewriting at the `build_resume_sections` seam and add a small `(profile, JD) → expected grounded bullets` eval set to guard truthfulness.

## Recommended reading order

1. `docs/handoffs/2026-06-19-end-of-day.md` (this file)
2. `docs/handoffs/2026-06-18-end-of-day.md` (prior session)
3. `README.md`
4. `AGENTS.md`

## Workspace

Clean at close — only source changes are tracked; build artifacts (`.venv`, `node_modules`, `dist`, `*.db`, `__pycache__`, `logs`) are gitignored, and no deployment zip remains in the tree.
